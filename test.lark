fib($x)=fib($x-1)+fib($x-2)
fib(1)=1
fib(2)=1


sock = socket()
nick = "LarkBot"
host ="irc.pdgn.co"
port = 6697
main = {sock.open "irc.pdgn.co" 6697
        sock.send ("NICK " + nick +"\r\n")
        (sock.send ("USER " + nick + " "+host+" bla :" + nick + "\r\n")
        ((sock.recv 1024)
      }
